import './ShotDetail.css'
import { useParams } from "react-router-dom";

const ShotDetail = () => {
	const { id } = useParams();

	return (
		<div id="Shot-Detail">

		</div>
	)
}

export default ShotDetail;
